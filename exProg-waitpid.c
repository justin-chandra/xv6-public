#include "param.h"
#include "user.h"
#include "types.h"
#include "stat.h"
#include "fs.h"
#include "fcntl.h"
#include "syscall.h"
#include "traps.h"
#include "memlayout.h"
#include "user.h"

#define NULL 0


int main() {
    /*
    // int stdout = 1;
    // int pid_t; 
    int pid1 = 0, pid2 = 0;
    // int i = 3;

    pid1 = fork();

    if(pid1 != 0) {
        // printf(stdout, (char*) ++i);
        pid2 = fork();
        if(pid2 != 0) {
            waitpid(pid1, NULL, 0);
            // printf(stdout, (char*) ++i);
            waitpid(pid2, NULL, 0);
            // printf(stdout, (char*) ++i);
        }

        else {
            fork();

            // printf(stdout, (char*) ++i);
        }

        exit(0);
    }
    // printf(stdout, (char*) ++i);
    */
    return 0;
}

